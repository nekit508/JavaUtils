apply plugin: "java"

ext {
    jarName = "JavaUtils"
    jarPath = "C:\\Users\\Никита\\Desktop"

    vers = "v3.3"
}

repositories {
    mavenCentral()
    maven { url "https://jitpack.io" }
}

wrapper {
    gradleVersion = '7.4.2'
}

jar {
    archiveFileName = "${jarPath}\\${jarName}-${vers}.jar"
}

dependencies {

}